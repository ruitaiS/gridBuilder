!clean up the writestate method; add more options as far as sepearators (see below)

also how to output the file, specifying:
    column / row delineator characters
    strings signifying array elements

-> this also needs to be updated in the java file as well
